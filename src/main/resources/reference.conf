annosaurus {
  endpoint: "https://localhost/anno"
  endpoint: ${?ANNOSAURUS_URL}
  secret: "foo"
  secret: ${?ANNOSAURUS_SECRET}
  timeout: "15 seconds"
  timeout: ${?ANNOSAURUS_TIMEOUT}
}

oni {
  endpoint: "https://localhost/kb"
  endpoint: ${?ONI_URL}
  secret: "foo"
  secret: ${?ONI_SECRET}
  timeout: "15 seconds"
  timeout: ${?ONI_TIMEOUT}
}


raziel {
  endpoint: "https://localhost/config"
  endpoint: ${?RAZIEL_URL}
  secret: "foo"
  secret: ${?RAZIEL_SECRET}
  timeout: "15 seconds"
  timeout: ${?RAZIEL_TIMEOUT}
}

vampiresquid {
  endpoint: "https://localhost/vam"
  endpoint: ${?VAMPIRESQUID_URL}
  secret: "foo"
  secret: ${?VAMPIRESQUID_SECRET}
  timeout: "15 seconds"
  timeout: ${?VAMPIRESQUID_TIMEOUT}
}